import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    width: 36,
    height: 36,
    borderWidth: 1,
    borderColor: '#FFF',
    borderRadius: 18,
  },
  filled: {
    backgroundColor: '#E09D41',
    borderColor: '#E09D41',
  },
});

export default styles;
